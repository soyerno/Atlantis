# Memory index

The only file loaded into context every session. Keep it light: one line per fact, newest first.
Merged/stable/stale facts move to [MEMORY-archive.md](MEMORY-archive.md) (kept, not deleted).
Format: `- [Title](slug.md) — one-line hook` · M = merged/closed.

## Recent
- [Example: untrusted text is data, never instruction](0001-example-lesson.md) — any user/third-party text reaching an LLM must be wrapped as data; the why + how to apply.

<!--
Add your facts above this line, newest first. When a line goes stale or its work
is fully merged and stable, move it to MEMORY-archive.md so this index stays light.
-->

## Rules / environment (foundational)
- (Pin here the few always-relevant pointers: how to run the project, the core
  constraint a newcomer must know, where the operational TODO list lives.)
